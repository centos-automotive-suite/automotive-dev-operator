---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: imagesealeds.automotive.sdv.cloud.redhat.com
spec:
  group: automotive.sdv.cloud.redhat.com
  names:
    kind: ImageSealed
    listKind: ImageSealedList
    plural: imagesealeds
    singular: imagesealed
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .spec.operation
      name: Operation
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          ImageSealed is the Schema for the imagesealeds API.
          It triggers an AIB sealed operation (prepare-reseal, reseal, extract-for-signing, inject-signed) on a disk image.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ImageSealedSpec defines the desired state of ImageSealed
            properties:
              aibExtraArgs:
                description: AIBExtraArgs are extra arguments to pass to AIB
                items:
                  type: string
                type: array
              aibImage:
                description: AIBImage is the automotive-image-builder container image
                  to use
                type: string
              inputRef:
                description: InputRef is the OCI reference to the input disk image
                type: string
              operation:
                description: 'Operation is the AIB sealed operation: prepare-reseal,
                  reseal, extract-for-signing, inject-signed'
                enum:
                - prepare-reseal
                - reseal
                - extract-for-signing
                - inject-signed
                type: string
              outputRef:
                description: OutputRef is the OCI reference where to push the result
                  (optional for extract-for-signing)
                type: string
              secretRef:
                description: |-
                  SecretRef is the name of the secret containing registry credentials
                  (REGISTRY_URL, REGISTRY_USERNAME, REGISTRY_PASSWORD)
                type: string
              signedRef:
                description: SignedRef is the OCI reference to signed artifacts; required
                  when operation is inject-signed
                type: string
              storageClass:
                description: StorageClass for the workspace (optional)
                type: string
            required:
            - inputRef
            - operation
            type: object
          status:
            description: ImageSealedStatus defines the observed state of ImageSealed
            properties:
              completionTime:
                description: CompletionTime is when the sealed operation completed
                format: date-time
                type: string
              message:
                description: Message provides additional details about the current
                  phase
                type: string
              outputRef:
                description: OutputRef is the OCI reference where the result was pushed
                  (after completion)
                type: string
              phase:
                description: Phase represents the current phase of the sealed operation
                enum:
                - Pending
                - Running
                - Completed
                - Failed
                type: string
              startTime:
                description: StartTime is when the sealed operation started
                format: date-time
                type: string
              taskRunName:
                description: TaskRunName is the name of the Tekton TaskRun executing
                  this sealed operation
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

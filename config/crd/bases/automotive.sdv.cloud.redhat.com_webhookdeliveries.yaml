---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: webhookdeliveries.automotive.sdv.cloud.redhat.com
spec:
  group: automotive.sdv.cloud.redhat.com
  names:
    kind: WebhookDelivery
    listKind: WebhookDeliveryList
    plural: webhookdeliveries
    singular: webhookdelivery
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WebhookDelivery is internal durable notification state, not a
          public trigger API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WebhookDeliverySpec contains internal references only; endpoint
              and key belong in the Secret.
            properties:
              callbackSecretRef:
                maxLength: 253
                minLength: 1
                type: string
              subject:
                description: DeliverySubject identifies an operation in the delivery's
                  own namespace by UID.
                properties:
                  apiVersion:
                    enum:
                    - automotive.sdv.cloud.redhat.com/v1alpha1
                    - tekton.dev/v1
                    type: string
                  kind:
                    enum:
                    - ImageBuild
                    - TaskRun
                    type: string
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                  uid:
                    description: |-
                      UID is a type that holds unique ID values, including UUIDs.  Because we
                      don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                      intent and helps make sure that UIDs and names do not get conflated.
                    maxLength: 128
                    minLength: 1
                    type: string
                required:
                - apiVersion
                - kind
                - name
                - uid
                type: object
                x-kubernetes-validations:
                - message: subject must be an ImageBuild or a Tekton v1 TaskRun
                  rule: (self.kind == 'ImageBuild' && self.apiVersion == 'automotive.sdv.cloud.redhat.com/v1alpha1')
                    || (self.kind == 'TaskRun' && self.apiVersion == 'tekton.dev/v1')
            required:
            - callbackSecretRef
            - subject
            type: object
            x-kubernetes-validations:
            - message: delivery intent is immutable
              rule: self == oldSelf
          status:
            description: WebhookDeliveryStatus stores a bounded snapshot and the latest
              attempt, not an unbounded history.
            properties:
              attempts:
                format: int32
                maximum: 1000
                minimum: 0
                type: integer
              completionTime:
                format: date-time
                type: string
              lastAttemptTime:
                format: date-time
                type: string
              lastError:
                description: LastError is a sanitized diagnostic, never a raw HTTP
                  error or response body.
                maxLength: 1024
                type: string
              lastHttpStatus:
                format: int32
                maximum: 599
                minimum: 100
                type: integer
              nextAttemptTime:
                format: date-time
                type: string
              snapshot:
                description: WebhookEventSnapshot freezes the exact JSON bytes that
                  are signed on every attempt.
                properties:
                  body:
                    description: Body is base64-encoded in Kubernetes JSON, preserving
                      exact payload bytes.
                    format: byte
                    maxLength: 349528
                    minLength: 1
                    type: string
                  id:
                    maxLength: 128
                    minLength: 1
                    type: string
                  time:
                    format: date-time
                    type: string
                  type:
                    enum:
                    - build.terminal
                    - flash.terminal
                    type: string
                required:
                - body
                - id
                - time
                - type
                type: object
                x-kubernetes-validations:
                - message: event snapshot is immutable
                  rule: self == oldSelf
              state:
                description: DeliveryState describes notification delivery independently
                  of the operation outcome.
                enum:
                - Pending
                - Delivering
                - Delivered
                - Failed
                type: string
            required:
            - attempts
            - state
            type: object
            x-kubernetes-validations:
            - message: event snapshot cannot be removed
              rule: '!has(oldSelf.snapshot) || has(self.snapshot)'
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
